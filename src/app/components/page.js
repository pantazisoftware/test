import { Header } from "@/components/blocks/header";

export default async function Components() {
  return (
    <main>
      <Header />
      <div>
        <p>Components Page</p>
      </div>
    </main>
  );
}
